ver container = '';

function preview(){
  let x = "";
  if(x != "/n"){
    let html = $(".ql-editor").html(); // .getHTML() は存在しないので .html() を使う
    $('#preview-container').html(html);
    console.log(x);
    let title = "";
    title += "";
  }
}
